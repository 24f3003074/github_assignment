- Create new folder in mac
- Initialize git repo
- Create a python file and write add function of two numbers(a, b)
- Using terminal, commit that file
- Create new repo in github
- Add remote url of that repo, in your git repo, then push the same file to github and check it
- Add new file to print - "Raiyyan will top Stats2", then push using VSCode

---

- Go to Github in the same repo
- Modify that file to subtract function, then add a new python file for multiply and divide
- Pull the changes using terminal
- Again go to Github, add new python file to print Hello World
- Pull the changes using VSCode

---

- Create new folder called "directory"
- Create new file called .env
- Create new python file having function to take two numbers(a,b) and find a^b
- Create three markdown files(ending .md)
- Ignore all md files and .env file and directory folder

---

- Create new branch called "prime-numbers"
- Switch to that branch
- Create new python file having "is_prime" function that takes a number and return True if it is prime, else False
- Commit and push that branch to Github using terminal
- Verify the changes in Github
- Create a pull request for those changes
- Merge that pull request and verify if the changes are made